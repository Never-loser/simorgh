# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Bundled engine + data are copied in by scripts/bundle-engine.mjs from the
# repo's canonical build. They are build inputs, not source: the engine binary
# and the 12 MB opening book do not belong in git.
src-tauri/resources/engine/

# Tauri generated schemas/metadata
src-tauri/gen/
